.env
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
.DS_Store
*.db
/data/

# Local proposals
examples/standards/VS Code Event Shipper — Proposal.md

# MCP config (device-specific)
.mcp.json
!plugins/**/.mcp.json
deploy/gcp/terraform.tfvars
deploy/gcp/.terraform/
deploy/gcp/*.tfstate
deploy/gcp/*.tfstate.backup
# Terraform-rendered startup scripts (contain secrets — never commit)
deploy/gcp/rendered_startup.sh
deploy/gcp/rendered_*.sh

# render-metadata.sh outputs (contain secrets)
startup-rendered.sh
metadata-backup-*.sh

# Landing page local archive (pre-edit snapshots, not for git)
web/landing/_archive/
