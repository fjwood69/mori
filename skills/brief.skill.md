- name: brief
- description: Session bootstrap — load shared knowledge, get oriented

1. Pull latest config: `git -C ~/moku-config pull 2>/dev/null || true`
2. Call `moku-memory_list` to see shared memories.
3. Read any that look relevant.
4. Report "Ready" when done. Do not take autonomous actions.